{
  "name": "@authenticator/core",
  "version": "0.1.0",
  "private": true,
  "description": "Platform-agnostic account storage, encryption, TOTP/HOTP generation, and import logic shared by the CLI, desktop, and mobile authenticator-clui clients",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p . && mkdir -p dist/importers && cp src/importers/google_auth.proto dist/importers/google_auth.proto",
    "test": "jest"
  },
  "dependencies": {
    "fs-extra": "^11.3.6",
    "protobufjs": "^8.7.1"
  },
  "devDependencies": {
    "@swc/core": "^1.15.43",
    "@swc/jest": "^0.2.39",
    "@types/fs-extra": "^11.0.4",
    "@types/jest": "^30.0.0",
    "@types/node": "^26.1.1",
    "jest": "^30.4.2",
    "typescript": "^7.0.2"
  }
}
