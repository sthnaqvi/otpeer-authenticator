# OTPeer logo — selected

**Selected: A5** — [`logo-a5-lockup-shield.png`](logo-a5-lockup-shield.png)

Shield + three bars (authenticator mark) with OTPeer / AUTHENTICATOR lockup.

```
--bg:      #1A1D21
--accent:  #5B9FD4
--fg:      #FFFFFF
--dim:     #9AA3A7
```

Cut assets live in [`final-from-a5/`](final-from-a5/). Production install:
`packages/desktop/scripts/generate_brand_assets.py` → `build/` (app icon, tray)
and `src/renderer/assets/` (mark, favicon).
